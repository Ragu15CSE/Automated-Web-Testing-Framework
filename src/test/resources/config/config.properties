# Application & Environment Details
url=https://www.saucedemo.com/
environment=QA

# Browser Settings (chrome / firefox / edge)
browser=chrome
headless=false

# Timeouts in Seconds
implicit.wait=10
explicit.wait=20
page.load.timeout=30

# Test Credentials
valid.username=standard_user
valid.password=secret_sauce
locked.username=locked_out_user

# Reporting & Screenshot Settings
screenshot.on.failure=true
screenshot.on.every.step=false
